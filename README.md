# LLM (Large Language Model) Proxy

## Overview 
The goal of the LLM proxy is to simplify the use and mangement of using multiple language models.

```python
llmproxy.route(prompt="What is 1+1?")
> 2 
```


